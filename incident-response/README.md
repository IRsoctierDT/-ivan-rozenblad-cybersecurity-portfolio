# Incident Response

Incident analysis and response deliverables — reconstructing events from evidence,
documenting timelines, root cause, and remediation.

| Artifact | Description |
|---|---|
| `dns-icmp-incident-report.docx` | DNS/ICMP outage: a workstation's UDP query to port 53 returned an ICMP "Destination Unreachable – Port Unreachable," halting name resolution. Summary, packet-level analysis, root cause, remediation. |
| `network-traffic-analysis-incident-report.docx` / `.pdf` | Incident report reconstructing an event from captured network traffic — timeline, indicators, root cause, and response actions. |

> **Related:** the Markdown writeup and raw packet capture for the DNS/ICMP incident live in
> [`../network-security/`](../network-security/).
